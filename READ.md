## Git Commit规范

```bash
# feat: 新功能
# fix: 修复bug
# perf: 性能优化
# refactor: 重构代码
# docs: 文档更新
# style: 代码格式

git commit -m 'fix: add xxxxx'
# 忽略提交规范
git commit -m 'feat: 商品管理页面 UI' --no-verify

pnpm run dev:antd
```

## 商品管理 - SKU 库存管理功能

### 功能清单

1. **动态属性加载**：根据商品分类自动获取 SKU 规格属性及组合
2. **智能添加 SKU**：
   - 单个添加：从下拉菜单选择规格组合添加
   - 一键添加：批量添加所有可用规格组合
   - 过滤机制：已添加的规格不再显示
   - 无可用组合时按钮禁用
3. **误删数据恢复**：编辑模式下添加 SKU 时自动从后端恢复原始数据（价格/库存/编码/主图/状态）
4. **SKU 编辑**：支持编辑 SKU 编码、价格、库存、会员折扣、状态、主图
5. **SKU 状态管理**：
   - 状态字段：启用(1)/禁用(0)，默认启用
   - Switch 开关控制，显示"启用/禁用"文字
6. **删除 SKU**：
   - 编辑模式：确认弹窗 → 调用 API 真实删除（通过 name 查找后端 ID）→ 删除列表数据
   - 新建模式：直接删除列表数据
   - 订单关联校验：后端检查订单关联，有关联则拒绝删除
7. **自动排序**：列表变化后自动按 `name` 升序排列
8. **分类切换提示**：修改商品分类后 SKU 属性自动更新，已存在的 SKU 数据不删除
